All new plugins (tuleap-plugin-fusionforge-compat, tuleap-plugin-doaprdf,
tuleap-plugin-admssw, tuleap-plugin-archivedeleteditems,
tuleap-plugin-boomerang, tuleap-plugin-foafprofiles, tuleap-plugin-openid,
tuleap-plugin-proftpd, tuleap-plugin-mediawiki)  have not been tested and
might need further integration work but the following issues are already
known:

 * The Debian package of mediawiki needs to be updated to >= 1.20
   and must be tested against tuleap-plugin-mediawiki.
 * The php-http-download package must be created (otherwise the -proftpd
   plugin in uninstallable).
 * The php-guzzle package must be created (otherwise the -git plugin
   is uninstallable).
