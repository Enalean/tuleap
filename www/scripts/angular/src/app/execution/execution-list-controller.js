angular
    .module('execution')
    .controller('ExecutionListCtrl', ExecutionListCtrl);

ExecutionListCtrl.$inject = ['$scope', 'SharedPropertiesService'];

function ExecutionListCtrl($scope, SharedPropertiesService) {
}