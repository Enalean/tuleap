{{^ is_properly_configured }}
    <div class="alert alert-info"><strong>{{ misconfigured_title }}</strong> {{ misconfigured_message }}</div>
    <img src="yuno.jpg" />
{{/ is_properly_configured }}
{{# is_properly_configured }}
    <div id="trafficlights-app" ng-app="trafficlights" ng-controller="TrafficlightsCtrl" ng-init="init('{{ nodejs_server }}', {{ project_id }}, {{ tracker_ids }}, '{{ lang }}', {{ current_user }}, {{ current_milestone }})">
        <div ncy-breadcrumb></div>
        <ui-view></ui-view>
    </div>
    <script src="scripts/angular/bin/assets/socket.io.js"></script>
    <script src="scripts/angular/bin/assets/trafficlights.js"></script>
{{/ is_properly_configured }}
