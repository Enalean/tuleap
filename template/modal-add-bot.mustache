<div id="admin-config-bot-add"
    class="modal hide fade"
    tabindex="-1"
    role="dialog"
    aria-labelledby="admin-config-bot-add-title"
    aria-hidden="true"
>

    <div class="modal-header">
        <a role="button" class="close" data-dismiss="modal"><i class="icon-remove"></i></a>
        <h3 id="configuration-add-bot-title" class="modal-configuration-bot-title">{{ modal_add_title }}</h3>
    </div>

    <form method="POST" action="/plugins/botmattermost_agiledashboard/?action=add_bot">
        {{# csrf_token }}
            {{> csrf_token_input }}
        {{/ csrf_token }}
        <input type="hidden" name="group_id" value="{{ project_id }}" />

        <div class="modal-body">
            {{# bot_list_is_empty }}
                <p>{{ empty_bot_list }}</p>
            {{/ bot_list_is_empty }}
            {{^ bot_list_is_empty }}
                <div class="form-group">
                    <label class="control-label col-sm-2" for="select_bot">Bot list:</label>
                    <div class="col-sm-8">
                        <select name="bot_id" class="form-control" id="select_bot">
                            {{# bots }}
                                <option {{# getAvatarUrl }}data-image="{{ getAvatarUrl }}"{{/ getAvatarUrl }}
                                        value="{{ getId }}">{{ getName }}</option>
                            {{/ bots}}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-sm-2" for="bot-mattermost-channels-names">{{ label_channels_handles }}</label>
                    <div class="col-sm-8 form-control">
                        <input id="add_channels"
                               class="input_channels form-control"
                               name="channels"
                               placeholder="{{ input_channels_handles }}"
                               hidden
                               multiple
                        />
                    </div>
                    <p class="text-info"><i class="icon-info-sign"></i> {{{ purified_info_channels_handles }}}</p>
                </div>
                <div id="field-bot-mattermost-agiledashboard-send-time" class="form-group">
                    <label class="control-label" for="bot-mattermost-agiledashboard-send-time">{{ label_send_time }}</label>
                    <div id="bot-mattermost-agiledashboard-send-time" class="input-append form-control" >
                        <span class="bot-mattermost-agiledashboard-field-time">
                            <input required
                                   name="send_time"
                                   class="input-small form-control"
                                   data-format="hh:mm"
                                   type="text"
                                {{# bot_assigned }}
                                   value="{{ send_time }}"
                                {{/ bot_assigned }}
                            >
                            </input>
                            <span class="add-on">
                                <i class="icon-time"></i>
                            </span>
                        </span>
                    </div>
                    <p class="text-warning"><i class="icon-warning-sign"></i> {{ alert_time_warning }}</p>
                </div>
            {{/ bot_list_is_empty }}
        </div>

        <div class="modal-footer">
            <button class="btn" data-dismiss="modal" aria-hidden="true">{{ button_close }}</button>
            <button type="submit" class="btn btn-primary">{{ button_confirm }}</button>
        </div>
    </form>
</div>