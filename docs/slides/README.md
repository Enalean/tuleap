# Tuleap technical slides

## How to open them?

Go inside this directory and run `pnpm run dev:<slidedeck_name>` where `slidedeck_name` is one of the following:
* `life-tuleap-request`
* `prosemirror-architecture`
