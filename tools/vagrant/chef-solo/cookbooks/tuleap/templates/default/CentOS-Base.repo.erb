# CentOS-Base.repo
#
# The mirror system uses the connecting IP address of the client and the
# update status of each mirror to pick mirrors that are updated to and
# geographically close to the client.  You should use this for CentOS updates
# unless you are manually picking other mirrors.
#
# If the mirrorlist= does not work for you, as a fall back you can try the 
# remarked out baseurl= line instead.
#
#

[base]
name=CentOS-$releasever - Base
<% if @mirror %>
baseurl=<%= @mirror %>/os/$basearch/
<% else %>
mirrorlist=http://mirrorlist.centos.org/?release=$releasever&arch=$basearch&repo=os
<% end %>
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-5
<% if @php_base == 'php53' %>
# Prevent PHP 5.1 conflicts
exclude=php php-*
<% else %>
# Prevent PHP 5.3 conflicts
exclude=php53 php53-*
<% end %>

#released updates 
[updates]
name=CentOS-$releasever - Updates
<% if @mirror %>
baseurl=<%= @mirror %>/updates/$basearch/
<% else %>
mirrorlist=http://mirrorlist.centos.org/?release=$releasever&arch=$basearch&repo=updates
<% end %>
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-5
<% if @php_base == 'php53' %>
# Prevent PHP 5.1 conflicts
exclude=php php-*
<% else %>
# Prevent PHP 5.3 conflicts
exclude=php53 php53-*
<% end %>

#additional packages that may be useful
[extras]
name=CentOS-$releasever - Extras
<% if @mirror %>
baseurl=<%= @mirror %>/extras/$basearch/
<% else %>
mirrorlist=http://mirrorlist.centos.org/?release=$releasever&arch=$basearch&repo=extras
<% end %>
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-5
<% if @php_base == 'php53' %>
# Prevent PHP 5.1 conflicts
exclude=php php-*
<% else %>
# Prevent PHP 5.3 conflicts
exclude=php53 php53-*
<% end %>

#additional packages that extend functionality of existing packages
[centosplus]
name=CentOS-$releasever - Plus
<% if @mirror %>
baseurl=<%= @mirror %>/centosplus/$basearch/
<% else %>
mirrorlist=http://mirrorlist.centos.org/?release=$releasever&arch=$basearch&repo=centosplus
<% end %>
gpgcheck=1
enabled=0
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-5
<% if @php_base == 'php53' %>
# Prevent PHP 5.1 conflicts
exclude=php php-*
<% else %>
# Prevent PHP 5.3 conflicts
exclude=php53 php53-*
<% end %>

#contrib - packages by Centos Users
[contrib]
name=CentOS-$releasever - Contrib
<% if @mirror %>
baseurl=<%= @mirror %>/contrib/$basearch/
<% else %>
mirrorlist=http://mirrorlist.centos.org/?release=$releasever&arch=$basearch&repo=contrib
<% end %>
gpgcheck=1
enabled=0
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-5
<% if @php_base == 'php53' %>
# Prevent PHP 5.1 conflicts
exclude=php php-*
<% else %>
# Prevent PHP 5.3 conflicts
exclude=php53 php53-*
<% end %>
