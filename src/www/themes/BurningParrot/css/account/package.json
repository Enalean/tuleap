{
  "name": "@tuleap/account-theme",
  "description": "Tuleap BurningParrot theme for user account",
  "author": "Enalean",
  "license": "GPL-2.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {},
  "config": {
    "bin": "../../../../../../node_modules/.bin"
  },
  "scripts": {
    "build": "$npm_package_config_bin/webpack --config webpack.prod.js",
    "watch": "$npm_package_config_bin/webpack --config webpack.dev.js --watch"
  }
}
