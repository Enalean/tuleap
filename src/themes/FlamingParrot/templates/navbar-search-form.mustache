{{# user_can_search }}
    {{# search_form_presenter}}
        <form action="/search/" method="get" class="navbar-search pull-left">
            {{# hidden_fields }}
                <input type="hidden" name="{{ name }}" value="{{ value }}" />
            {{/ hidden_fields }}
            <input type="hidden" name="type_of_search" value="{{{ selected_entry }}}" />
            <input type="hidden" name="number_of_page_results" value="{{ number_of_page_results }}">
            <input type="hidden" name="exact" value="0" />

            <div class="input-append">
                <input id="navbar-search-bar" type="text" name="words" class="span2" placeholder="{{ search_placeholder }}" aria-label="{{ search_label }}"/>
            </div>
        </form>
    {{/ search_form_presenter}}
{{/ user_can_search }}
