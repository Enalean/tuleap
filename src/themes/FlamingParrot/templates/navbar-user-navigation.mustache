<ul class="nav pull-right header-navbar-container-user-nav">
    {{> navbar-search-form }}

    {{> join-tuleap-community }}

    {{# is_project_registration_enabled }}

        <li id="nav-dropdown-new" class="dropdown navbar-new-dropdown">
            <button type="button" class="dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-plus nav-dropdown-new-icon" aria-hidden="true"></i>
                {{# gettext }}New{{/gettext}}
            </button>
            <ul class="dropdown-menu pull-right">
                <li>
                    <div class="nav-dropdown-content-item-title">Tuleap</div>
                    <a href="/project/new" class="nav-dropdown-content-item">
                        <i class="fa fa-fw fa-archive nav-dropdown-content-user-link-icon" aria-hidden="true"></i>
                        {{# gettext }}Start a new project{{/ gettext }}
                    </a>
                </li>
            </ul>
        </li>
    {{/ is_project_registration_enabled }}

    {{# user_is_logged_in }}
        {{# user_actions }}
            <li>
                <a {{# id }}id="user-action-{{ id }}"{{/ id }} href="{{ href }}">
                    {{# icon }}<i class="{{ icon }}"></i>{{/ icon }}
                    {{ label }}
                </a>
                {{{ purified_content }}}
            </li>
        {{/ user_actions }}
        {{> navbar-user-logged-in }}
    {{/ user_is_logged_in }}
    {{^ user_is_logged_in }}
        <li>
            <a id="navbar-user-login" href="{{ login_url }}">
                {{ include_menu_login_text }}
            </a>
        </li>
        {{# display_new_user }}
        <li>
            <a href="/account/register.php">
                {{ include_menu_new_user_text }}
            </a>
        </li>
        {{/ display_new_user }}
    {{/ user_is_logged_in }}
</ul>
