{{#hasSidebar}}
    {{# project_context }}
        {{> project-sidebar}}
    {{/ project_context }}
    <div class="main container-fluid">
{{/hasSidebar}}
{{^hasSidebar}}
    <div class="main full container-fluid">
{{/hasSidebar}}

{{# switch_to }}
    <div id="switch-to-mount-point"
         data-projects="{{ projects }}"
         data-is-trove-cat-enabled="{{ is_trove_cat_enabled }}"
         data-are-restricted-users-allowed="{{ are_restricted_users_allowed }}"
         data-is-search-available="{{ is_search_available }}"
    ></div>
{{/ switch_to }}

{{# project_context }}
    {{# has_project_banner }}
        <div class="project-banner {{^ project_banner_is_visible }}project-banner-hidden{{/ project_banner_is_visible }}">
            <p id="current-project-banner-message" class="project-banner-clamped">{{{ purified_banner }}}</p>
            {{# current_user_id }}
                <i
                        id="project-banner-close"
                        class="fa fa-close"
                        aria-label="{{# gettext }}Close{{/ gettext }}"
                        data-project-id="{{ project_id }}"
                        data-user-id="{{ current_user_id }}"
                ></i>
            {{/ current_user_id }}
        </div>
    {{/ has_project_banner }}
{{/ project_context }}
    <div class="row-fluid">
        <div class="span12">
            {{#hasBreadcrumbs}}
                {{> breadcrumbs/breadcrumbs }}
            {{/hasBreadcrumbs}}

            {{> toolbar}}

            {{{feedback}}}

            <div class="content">
