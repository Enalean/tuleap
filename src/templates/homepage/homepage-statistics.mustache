{{# statistics }}
    {{# hasStatistics }}
        <div>
            {{# getStatistics }}
                <div class="tlp-card">
                    <div>
                        {{# hasGrowth }}
                            {{ getLastMonthGrowth }}
                            {{# gettext }}last month{{/ gettext }}
                        {{/ hasGrowth }}
                    </div>

                    <p>
                        {{ getTotal }} {{ getLabel }}
                    </p>
                </div>
            {{/ getStatistics }}
        </div>
    {{/ hasStatistics }}
{{/ statistics }}