<h1>{{# gettext }}Password policy{{/ gettext }}</h1>

<section class="tlp-pane">
    <div class="tlp-pane-container">
        <div class="tlp-pane-header">
            <h1 class="tlp-pane-title">
                <i class="fa fa-key tlp-pane-title-icon"></i>{{# gettext }}Compromised password{{/ gettext }}
            </h1>
        </div>
        <section class="tlp-pane-section">
            <p>
                {{# gettext }}This feature allows you to prevent the usage of passwords commonly found in known data breaches as these passwords are privileged targets.{{/ gettext }}
                {{# gettext }}If your instance can not access the Internet, it is preferable to disable the verification to avoid a slowness during password compliance check.{{/ gettext }}
                <a href="https://haveibeenpwned.com/Passwords">{{# gettext }}To determine if a password is compromised, the Have I Been Pwned service is used.{{/ gettext }}</a>
            </p>

            <div class="tlp-form-element">
                <label class="tlp-label" for="block-breached-password">
                    {{# gettext }}Block usage of passwords known as compromised{{/ gettext }}
                </label>
                <div class="tlp-switch">
                    <input type="checkbox" disabled id="block-breached-password" class="tlp-switch-checkbox"
                           {{# is_password_compromised_check_enabled }}checked{{/ is_password_compromised_check_enabled }}
                    >
                    <label for="block-breached-password" class="tlp-switch-button">{{# gettext }}Block usage of passwords known as compromised{{/ gettext }}</label>
                </div>
            </div>
        </section>
    </div>
</section>
