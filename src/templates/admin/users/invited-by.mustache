{{# invited_by }}
    <div class="tlp-property">
        <label class="tlp-label">{{# gettext }}Invited by{{/gettext}}</label>
        {{# has_been_invited }}
            {{# has_been_invited_by_only_one }}
                <p>
                    {{# invited_by_users }}
                        {{> invited-by-user }}
                    {{/ invited_by_users }}
                </p>
            {{/ has_been_invited_by_only_one }}
            {{^ has_been_invited_by_only_one }}
                <ul>
                    {{# invited_by_users }}
                        <li>
                            {{> invited-by-user }}
                        </li>
                    {{/ invited_by_users }}
                </ul>
            {{/ has_been_invited_by_only_one }}
        {{/ has_been_invited }}
        {{^ has_been_invited }}
            <span class="tlp-property-empty">{{# gettext }}Nobody{{/ gettext }}</span>
        {{/ has_been_invited }}
    </div>
{{/ invited_by }}
