<h3>{{ section_title }}</h3>
<div class="row-fluid project_access_label">
    <label for="project_visibility" class="span8">{{ choose_visbility }}</label>
</div>
<select name="project_visibility">
    {{# options }}
        <option value="{{ value }}" {{ selected }}>{{ label }}</option>
    {{/ options }}
</select>
<br>
{{# platform_allows_restricted }}
    <p class="alert alert-warning">
        {{{ restricted_warning_message }}}
    </p>
{{/ platform_allows_restricted }}
