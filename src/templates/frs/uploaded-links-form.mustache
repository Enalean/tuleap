<fieldset>
    <legend>{{ uploaded_links_label }}</legend>

    <table class="frs-uploaded-link-update" id="frs-uploaded-link-update">
        <thead>
        <tr class="boxtable">
            <td>{{ link_label }}</td>
        </tr>
        </thead>
        <tbody id="files_body">
        {{# existing_links }}
            <tr>
                <td><a href="{{ link }}">{{ displayed_link }}</a> </td>
            </tr>
        {{/ existing_links }}
        </tbody>
    </table>
</fieldset>
