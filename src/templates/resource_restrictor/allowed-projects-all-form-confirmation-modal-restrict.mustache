<div id="allowed-projects-all-form-confirmation-modal-restrict"
     class="tlp-modal tlp-modal-danger"
     role="dialog"
     aria-labelledby="allowed-projects-all-form-confirmation-modal-restrict-label"
>
    <div class="tlp-modal-header">
        <h1 class="tlp-modal-title" id="allowed-projects-all-form-confirmation-modal-restrict-label">
            {{# gettext }}Disallowing for all projects{{/ gettext }}
        </h1>
        <button class="tlp-modal-close" type="button" data-dismiss="modal" aria-label="{{# gettext }}Close{{/ gettext }}">
            <i class="fas fa-times tlp-modal-close-icon" aria-hidden="true"></i>
        </button>
    </div>
    <div class="tlp-modal-body">
        <p>
            {{# gettext }}You are about to disallow access to a resource for every projects on the platform.{{/ gettext }}
        </p>
        <p>{{# gettext }}Please confirm your action.{{/ gettext }}</p>
    </div>
    <div class="tlp-modal-footer">
        <button type="reset" class="tlp-button-danger tlp-button-outline tlp-modal-action" data-dismiss="modal">Cancel</button>
        <button type="submit" class="tlp-button-danger tlp-modal-action">
            <i class="tlp-button-icon fa fa-save" aria-hidden="true"></i>
            {{# gettext }}Disallow for all projects{{/ gettext }}
        </button>
    </div>
</div>
