{{# gettext }}Hello %s, | {{ user_real_name }}{{/ gettext }}

{{# gettext }}
    You invited %1$s with email %2$s to register to %3$s and join the project %4$s. |
    {{ invited_user }} |
    {{ invited_user_email }} |
    {{ instance_name }} |
    {{ project_name }}
{{/ gettext }}


{{# gettext }}
    Unfortunately we couldn't add %1$s as project member because they have restricted status and the project is configured to not accept restricted users. |
    {{ invited_user }}
{{/ gettext }}


{{# gettext }}Best regards,{{/ gettext }}

{{# gettext }}The %s team. | {{ instance_name }}{{/ gettext }}
