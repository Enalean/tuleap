<tlp-relative-date
    date="{{ date }}"
    absolute-date="{{ absolute_date }}"
    placement="{{ placement }}"
    preference="{{ preference }}"
    locale="{{ locale }}"
>{{ absolute_date }}</tlp-relative-date>
