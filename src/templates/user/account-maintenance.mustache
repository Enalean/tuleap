<div id="account-handler" class="row-fluid">
    <div id="account-maintenance" class="span12">
        <ul class="nav nav-tabs">
        {{# tabs }}
            <li {{# is_active }}class="active"{{/ is_active }}><a href="{{ href }}">{{ label }}</a></li>
        {{/ tabs }}
        </ul>

        {{> info }}
    </div>
</div>
