<div class="tlp-framed-vertically">
    {{# user_presenter }}
        <div class="tlp-framed-horizontally dashboard-user-title">
            <div class="tlp-avatar-jumbo">
                {{# has_avatar }}
                    <img src="{{ avatar_url }}">
                {{/ has_avatar }}
            </div>
            <h1 class="dashboard-user-title-name">
                {{ real_name }}
                <small class="dashboard-user-title-login">@{{ login }}</small>
            </h1>

        </div>
    {{/ user_presenter }}
    {{# has_dashboard }}
        {{> dashboard-tabs }}

        {{# dashboards }}
            {{# is_active }}
                {{# has_widget }}
                    {{> widgets }}
                {{/ has_widget }}
                {{^ has_widget }}
                    <div class="empty-page">
                        <p class="empty-page-text">{{ no_widget_label }}</p>
                        <p class="empty-page-text">{{{ purified_no_widget_action_label }}}</p>
                        <button
                                type="button"
                                id="no-widgets-edit-dashboard-button"
                                class="tlp-button-primary tlp-button-large"
                                data-target-modal-id="no-widgets-edit-dashboard-modal"
                        >
                            <i class="fa fa-plus tlp-button-icon"></i>
                            {{ edit_dashboard_label }}
                        </button>
                        {{> no-widgets-edit-dashboard-modal }}
                    </div>
                {{/ has_widget }}
            {{/ is_active }}
        {{/ dashboards }}
    {{/ has_dashboard }}
</div>

{{^ has_dashboard }}
    <div class="empty-page">
        <p class="empty-page-text">{{ no_dashboard_label }}</p>
        <button
            type="button"
            id="add-dashboard-button"
            class="tlp-button-primary tlp-button-large"
            data-target-modal-id="add-dashboard"
        >
            <i class="fa fa-plus tlp-button-icon"></i>
            {{ add_dashboard_label }}
        </button>
    </div>
{{/ has_dashboard }}

{{> add-dashboard-modal }}
{{# dashboards }}
    {{# is_active }}
        {{> delete-dashboard-modal }}
        {{> edit-dashboard-modal }}
    {{/ is_active }}
{{/ dashboards }}
