<script id="dashboard-add-widget-list-table-placeholder" type="x-tmpl-mustache">
    {{=<% %>=}}
        <table>
            {{# widgets_categories }}
                <tbody>
                    <tr>
                        <td class="tlp-table-cell-section" colspan="2">{{ name }}</td>
                    </tr>
                </tbody>
                <tbody>
                    {{# widgets }}
                        <tr class="dashboard-add-widget-list-table-widget" data-widget-id="{{ id }}" data-test="{{ id }}">
                            <td class="tlp-table-cell-filterable dashboard-add-widget-list-table-widget-content-name">
                                {{ name }}
                            </td>
                            <td class="dashboard-add-widget-list-table-widget-content-{{# is_used }}used{{/ is_used }}{{^ is_used }}link{{/ is_used }}">
                                {{# is_used }}
                                    {{ already_used_label }}
                                {{/ is_used }}
                                {{^ is_used }}
                                    <i class="fa fa-chevron-right dashboard-add-widget-list-table-widget-content-link-icon"></i>
                                {{/ is_used }}
                            </td>
                        </tr>
                    {{/ widgets }}
                </tbody>
        {{/ widgets_categories }}
        </table>
    <%={{ }}=%>
</script>
