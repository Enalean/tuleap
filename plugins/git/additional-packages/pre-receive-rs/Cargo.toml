[package]
name = "pre-receive-rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "prereceivers"
path = "src/lib.rs"
crate-type = ["cdylib"]

[build-dependencies]
cbindgen = "^0.6.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
