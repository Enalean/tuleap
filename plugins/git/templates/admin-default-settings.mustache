<div class="tab-pane active">
    <div class="tabbable" id="admin-general-settings">
        <ul class="nav nav-tabs">
            {{# panes }}
                <li {{# is_active }} class="active" {{/ is_active }}>
                    <a {{# is_disabled }}class="disabled-link"{{/is_disabled}} href="{{ href }}">{{ title }}</a>
                </li>
            {{/ panes }}
        </ul>
        <div class="tab-content">
            <div class="tab-pane active">
                {{# panes }}
                    {{# is_active }}
                        {{{ content }}}
                    {{/is_active}}
                {{/ panes }}
            </div>
        </div>
    </div>
</div>
