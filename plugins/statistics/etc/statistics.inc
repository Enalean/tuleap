<?php

// The statistics period 
$statistics_period = 3;
// The allowed quota disk size (size in GB)
$allowed_quota = 5;

// Maximum disk size quota allowed by project (size in GB)
$maximum_quota = 50;

?>