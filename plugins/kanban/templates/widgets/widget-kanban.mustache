{{# is_empty }}
    {{# there_is_error }}
        <div class="tlp-alert-danger">
            {{ error_message }}
        </div>
    {{/ there_is_error }}
    <section class="empty-state-pane">
    <p class="empty-state-text">{{ empty_state }}</p>
</section>
{{/ is_empty }}
{{^ is_empty }}
    {{# kanban_presenter }}
        <div class="widget-kanban"
             ng-controller="MainCtrl"
        >
            <div class="kanban-init-data" ng-non-bindable
                 data-user-id="{{ user_id }}"
                 data-kanban="{{ kanban_representation }}"
                 data-dashboard-dropdown="{{ dashboard_dropdown_representation }}"
                 data-user-is-admin="{{ user_is_kanban_admin }}"
                 data-widget-id="{{ widget_id }}"
                 data-view-mode="{{ view_mode }}"
                 data-kanban-url="{{ kanban_url }}"
                 data-kanban-homepage-url="{{ kanban_homepage_url }}"
                 data-tracker-reports="{{ tracker_reports }}"
                 data-language="{{ language }}"
                 data-user-accessibility-mode="{{ user_accessibility_mode }}"
                 data-kanban-mercure-enabled ="{{ mercure_enabled }}"
            ></div>
            <kanban class="kanban widget-context"></kanban>
        </div>
    {{/ kanban_presenter }}
{{/ is_empty }}
