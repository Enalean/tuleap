;
; VuFind Configuration
;

; LDAP is optional.  This section only needs to exist if the
; Authentication Method is set to LDAP.

[LDAP]
host            = myshib.franck.de
port            = 398
uid             = uid
basedn          = "ou=employees,dc=franck,dc=myshib,dc=de"
