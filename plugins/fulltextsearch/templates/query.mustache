<h1>Admin - Full Text Search</h1>

<p>This interface provide a way to access elasticsearch index, it's only for
site admin usage for the time being.</p>
<p><ul>
    <li>Index size: {{index_size}}</li>
    <li>Number of indexed documents: {{nb_docs}}</li>
</ul></p>

<form name="fts_query" method="get" action="?func=search">
    <p>Enter the terms you are looking for
        <input type="text" name="terms" value="{{terms}}" />
        <input type="submit" />
    </p>
</form>

{{#has_results}}
<p>{{result_count}} document(s) match:
    <ol>
    {{#search_results}}
        <li class="fts_result">
            <div class="fts_title">
                <a href="{{url}}">{{item_title}}</a> - {{project_name}}
            </div>
            <div class="fts_highlight">{{{highlight}}}...</div>
            <div class="fts_permissions">Permissions: {{permissions}}</div>
        </li>
    {{/search_results}}
    </ol>
</p>

<p>Query took {{elapsed_time}}s</p>
{{/has_results}}

{{#no_results}}
No results found
{{/no_results}}