#!/usr/bin/perl -U
#
# archive-deleted-items source_path destination_path
#

use warnings;
use strict;
use File::Copy;



if (!copy($ARGV[0], $ARGV[1])) {
    die("FAILURE: cannot move file ($!)\n");
}

print "OK\n";