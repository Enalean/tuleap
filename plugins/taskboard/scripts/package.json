{
  "author": "Enalean Team",
  "name": "tuleap-taskboard",
  "version": "1.0.0",
  "homepage": "https://www.enalean.com",
  "license": "GPL-2.0-or-later",
  "private": true,
  "dependencies": {
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "@vue/test-utils": "^1.0.0-beta.27",
    "vue-property-decorator": "^8.2.1"
  },
  "config": {
    "bin": "../../../node_modules/.bin/"
  },
  "scripts": {
    "build": "rm -rf ../../../src/www/assets/taskboard/scripts && NODE_ENV=production $npm_package_config_bin/webpack --mode=production",
    "watch": "NODE_ENV=watch $npm_package_config_bin/concurrently --raw --kill-others '$npm_package_config_bin/webpack --watch --mode=development' 'CI=true npm test -- --watch'",
    "test": "$npm_package_config_bin/jest"
  }
}
