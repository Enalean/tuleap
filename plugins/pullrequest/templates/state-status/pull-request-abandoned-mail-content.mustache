{{# dgettext }}
    tuleap-pullrequest |
    <a href="%s">%s</a> has closed the pull request <a href="%s">#%s</a>: %s |
    {{ change_user_profile_url }} |
    {{ change_user_display_name }} |
    {{ pull_request_url }} |
    {{ pull_request_id }} |
    {{ pull_request_title }}
{{/ dgettext }}
