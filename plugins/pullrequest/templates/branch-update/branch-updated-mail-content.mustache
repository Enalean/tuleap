<p>
    {{# dngettext }}
        tuleap-pullrequest |
        %s pushed %s commit updating the pull request %s: %s. |
        %s pushed %s commits updating the pull request %s: %s. |
        {{ nb_new_commits }} |
        <a href="{{ change_user_profile_url }}">{{ change_user_display_name }}</a> |
        {{ nb_new_commits }} |
        <a href="{{ pull_request_url }}">#{{ pull_request_id }}</a>  |
        {{ pull_request_title }}
    {{/ dngettext }}
</p>
<ul>
    {{# new_commits }}
        <li><a href="{{ url }}">{{ short_reference }}</a> {{ title }}</li>
    {{/new_commits}}
</ul>
