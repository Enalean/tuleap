<h1 class="agiledashboard-administration-title">{{# dgettext }} tuleap-agiledashboard | Agile Dashboard administration {{/ dgettext }}</h1>

<div class="main-project-tabs">
    <nav class="tlp-tabs">
        <a class="tlp-tab tlp-tab-active"
           href="/plugins/agiledashboard/?group_id={{ project_id }}&action=admin&pane=scrum">{{# dgettext }} tuleap-agiledashboard | Scrum {{/ dgettext }}
        </a>
        <a class="tlp-tab"
           href="/plugins/agiledashboard/?group_id={{ project_id }}&action=admin&pane=kanban">{{# dgettext }} tuleap-agiledashboard | Kanban {{/ dgettext }}
        </a>
        {{# can_burnup_be_configured }}
            <a class="tlp-tab"
               href="/plugins/agiledashboard/?group_id={{ project_id }}&action=admin&pane=charts">{{# dgettext }} tuleap-agiledashboard | Charts {{/ dgettext }}
            </a>
        {{/ can_burnup_be_configured }}
    </nav>
</div>

<div class="tlp-framed">
    <h2>{{ planning_name }}</h2>
    <section class="tlp-pane">
        <div class="tlp-pane-container">
            <div class="tlp-pane-header">
                <h1 class="tlp-pane-title">{{# dgettext }} tuleap-scaled_agile | Team Projects {{/ dgettext }}</h1>
            </div>
            <section class="tlp-pane-section">
                <p>
                    {{# dgettext }} tuleap-scaled_agile | The following projects contribute to <b>%s</b> by feeding the backlog with their plannable items. You can edit these by configuring the top planning of each of the following projects. | {{ project_name }} {{/ dgettext }}
                </p>
                <table class="tlp-table">
                    <thead>
                        <tr>
                            <th>{{# dgettext }} tuleap-scaled_agile | Team Project {{/ dgettext }}</th>
                            <th>{{# dgettext }} tuleap-scaled_agile | Plannable Items {{/ dgettext }}</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        {{^ plannable_items }}
                        <tr>
                            <td colspan="3" class="tlp-table-cell-empty">{{# dgettext }} tuleap-scaled_agile | There are no plannable items in any team projects. {{/ dgettext }}</td>
                        </tr>
                        {{/ plannable_items }}
                        {{# plannable_items }}
                            <tr>
                                <td>{{ project_name }}</td>
                                <td>
                                {{# plannable_item_presenters }}
                                    <span class="tlp-badge-{{ tracker_color }}">{{ tracker_name }}</span>
                                {{/ plannable_item_presenters }}
                                {{# configuration_link }}
                                <td>
                                    <a class="tlp-table-cell-actions-button tlp-button-small tlp-button-primary tlp-button-outline team-go-to-admin"
                                       href="{{ . }}"
                                    ><i class="fas fa-long-arrow-alt-right"></i> {{# dgettext }} tuleap-scaled_agile | Planning Configuration {{/ dgettext }}
                                    </a>
                                </td>
                                {{/ configuration_link }}
                            </tr>
                        {{/ plannable_items }}
                    </tbody>
                </table>
            </section>
        </div>
    </section>
</div>
