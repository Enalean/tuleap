<div id="frs-app"
    ng-app="tuleap.frs"
    ng-controller="AppController as $ctrl"
    ng-init="$ctrl.init({{ release_id }}, '{{ language }}')"
>
    <span ng-bind="$ctrl.itWorks">It does not work</span>
</div>
