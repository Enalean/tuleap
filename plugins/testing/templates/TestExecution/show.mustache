<ul class="nav nav-tabs">
  <li class="active"><a href="/plugins/testing/?group_id=1&resource=campaign">Campaigns</a></li>
  <li><a href="/plugins/testing/?group_id=1&resource=requirement">Requirements</a></li>
</ul>

<h2>{{# campaign }}<a href="{{ show_uri }}">{{ name }}</a> » {{/ campaign }}
    {{ name }} {{# last_result }}{{> label-status }}{{/ last_result }}
</h2>

<p><label>Assigned to:</label> {{ assignee }}</p>

<h3>Specification</h3>
<p><label>Execution type:</label> manual</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th>Step</th>
            <th>Expected result</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>1</td>
            <td>Go in project admin and type a user name in "Login Name" in project members part</li>
                </ol>
            </td>
            <td>The autocompleter displays matching users</td>
        </tr>
        <tr>
            <td>2</td>
            <td>
                Click on "Add User"
            </td>
            <td>
                <ul>
                    <li>the user to be added is in the list of project members</li>
                    <li>feedback info tells one new user is added "User added"</li>
                    <li>The new project-member receives a notification: Welcome to the project_name.</li>
                </ul>
            </td>
        </tr>
    </tbody>
</table>

<h3>Results</h3>
<form action="/plugins/testing/?group_id=1">
    <div class="form-actions">
        <table><tr>
            <td>
                <textarea placeholder="Notes, description, …" rows="10" cols="60"></textarea>
            </td>
            <td>
                <ul class="result_choices">
                    {{# last_result }}
                    <li><label><input type="radio" name="result" {{# is_not_run }}checked="checked"{{/ is_not_run }}> <span class="label" title="Not run">Not run</span></label></li>
                    <li><label><input type="radio" name="result" {{# is_passed }}checked="checked"{{/ is_passed }}> <span class="label label-success" title="Passed">Passed</label></li>
                    <li><label class="result_choice"><input type="radio" name="result" {{# is_failed }}checked="checked"{{/ is_failed }}> <span class="label label-important" title="Failed">Failed</label></li>
                    {{/ last_result }}
                </ul>
                <p><a href="#submit-new-artifact" class="tracker-submit-new"> log a bug</a></p>
                <p>
                    <button type="button" class="btn">Save</button>
                    <button type="button" class="btn">Save and continue</button>
                </p>
            </td>
        </tr></table>
    </div>
</form>

{{# last_result }}
    {{# executed_on }}
    <h3>Last result</h3>
    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th>Reporter</th>
                <th>Date</th>
                <th>Result</th>
                <th>Message</th>
            </tr>
        </thead>
        <tbody>
            <tr class="{{# is_passed}}success{{/ is_passed }}{{# is_failed}}error{{/ is_failed }}{{# is_not_run }}not_run_status_row{{/ is_not_run }}">
                <td>{{ executed_by }}</td>
                <td>{{ executed_on }}</td>
                <td>{{> label-status }}</td>
                <td class="span6">{{{ message }}}</td>
            </tr>
        </tbody>
    </table>
    {{/ executed_on }}
{{/ last_result }}
