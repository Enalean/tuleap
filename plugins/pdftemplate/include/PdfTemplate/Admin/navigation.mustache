{{# navigation }}
    <nav class="tlp-tabs">
        {{# panes }}
            <a href="{{ href }}" class="tlp-tab {{# is_active }}tlp-tab-active{{/ is_active }}">
                {{ label }}
            </a>
        {{/ panes }}
    </nav>
{{/ navigation }}
