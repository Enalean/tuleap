{
  "author": "Enalean Team",
  "name": "tuleap-admindelegation",
  "version": "1.0.0",
  "homepage": "https://www.enalean.com",
  "license": "GPL-2.0+",
  "private": true,
  "dependencies": {},
  "devDependencies": {},
  "config": {
    "bin": "../../../../node_modules/.bin"
  },
  "scripts": {
    "build": "rm -rf ../../../../src/www/assets/admindelegation/scripts/ && NODE_ENV=production $npm_package_config_bin/webpack --mode=production",
    "watch": "$npm_package_config_bin/webpack --watch --mode=development"
  }
}
