<div
    id="plan-iterations-app"
    class="empty-state-page"
    data-program="{{ program }}"
    data-program-flags="{{ program_flags }}"
    data-program-privacy="{{ program_privacy }}"
    data-program-increment="{{ program_increment }}"
    data-is-user-admin="{{ is_user_admin }}"
>
    <p class="empty-state-text">
        {{# dgettext }} tuleap-program_management | Nothing to see here for the moment {{/ dgettext }}
    </p>
</div>
