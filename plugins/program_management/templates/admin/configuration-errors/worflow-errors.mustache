{{# transition_rule_error }}
    <li>
        {{# dgettext }}
            tuleap-program_management |
            Following trackers are using a synchronized field in a workflow transition rule
        {{/ dgettext }}
        <a href='/plugins/tracker/workflow/{{ tracker_id }}/transitions'>#{{ tracker_id }}</a>
    </li>
{{/ transition_rule_error }}
{{# transition_rule_date_error }}
    <li>
        {{# dgettext }}
            tuleap-program_management |
            Following trackers are using a synchronized field in a workflow date rule (global rules)
        {{/ dgettext }}
        <a href='/plugins/tracker/workflow/{{ tracker_id }}/transitions'>#{{ tracker_id }}</a>
    </li>
{{/ transition_rule_date_error }}
{{# field_dependency_error }}
    <li>
        {{# dgettext }}
            tuleap-program_management |
            Following trackers are using a synchronized field in a workflow list rule (field dependencies)
        {{/ dgettext }}
        <a href='/plugins/tracker/workflow/{{ tracker_id }}/transitions'>#{{ tracker_id }}</a>
    </li>
{{/ field_dependency_error }}
