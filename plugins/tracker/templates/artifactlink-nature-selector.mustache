<select class="{{ select_classes }}" name="{{ select_name }}" data-test="artifact-link-nature-selector">
    <option value=""></option>
    {{# natures }}
        <option {{# is_selected }}selected{{/ is_selected }} value="{{ shortname }}">{{ forward_label }}</option>
    {{/ natures }}
</select>
