<div id="reply-by-mail-modal-info" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="reply-by-mail-modal-info-label" aria-hidden="true">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 id="reply-by-mail-modal-info-label">
            {{# dgettext }}tuleap-tracker | Reply artifact by email{{/ dgettext }}
        </h3>
    </div>
    <div class="modal-body">
        <p>{{# dgettext }}tuleap-tracker | You can send an email to the following email address in order to reply to this artifact:{{/ dgettext }}</p>
        <copy-to-clipboard
            value="{{ email }}"
            aria-label="{{# dgettext }} tuleap-tracker | Copy the email {{/ dgettext }}"
            class="input-append tracker-email-copy-to-clipboard"
        >
            <input type="text" value="{{ email }}" class="tracker-email-copy-to-clipboard-input input-xlarge" readonly>
            <button
                class="btn tracker-email-copy-to-clipboard-button"
                title="{{# dgettext }}tuleap-tracker | Copy to clipboard{{/ dgettext }}"
            ><i class="far fa-copy" aria-hidden="true"></i></button>
        </copy-to-clipboard>
        <p class="text-info tracker-email-copy-to-clipboard-info">
            {{# dgettext }}tuleap-tracker | The email has been copied to clipboard{{/ dgettext }}
        </p>
    </div>
    <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">{{# gettext }}Close{{/ gettext }}</button>
    </div>
</div>
