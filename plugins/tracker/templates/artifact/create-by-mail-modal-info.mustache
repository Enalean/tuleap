<div id="create-by-mail-modal-info" class="tlp-modal modal hide fade" tabindex="-1" role="dialog" aria-labelledby="reply-by-mail-modal-info-label" aria-hidden="true">
    <div class="tlp-modal-header modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h1 id="reply-by-mail-modal-info-label" class="tlp-modal-title">
            {{# dgettext }}tuleap-tracker | Create artifact by email{{/ dgettext }}
        </h1>
        <div class="tlp-modal-close" tabindex="0" role="button" data-dismiss="modal" aria-label="Close">
            ×
        </div>
    </div>
    <div class="tlp-modal-body modal-body">
        <p>{{# dgettext }}tuleap-tracker | You can send an email to the following email address in order to create an artifact:{{/ dgettext }}</p>
        <copy-to-clipboard
            value="{{ email }}"
            aria-label="{{# dgettext }} tuleap-tracker | Copy the email {{/ dgettext }}"
            class="tlp-form-element tlp-form-element-append input-append tracker-email-copy-to-clipboard"
        >
            <input
                type="text"
                value="{{ email }}"
                class="tracker-email-copy-to-clipboard-input input-xlarge tlp-input"
                readonly>
            <button
                class="btn tlp-append tlp-button-primary tlp-button-outline tracker-email-copy-to-clipboard-button"
                title="{{# dgettext }}tuleap-tracker | Copy to clipboard{{/ dgettext }}"
            ><i class="far fa-copy" aria-hidden="true"></i></button>
        </copy-to-clipboard>
        <p class="text-info tlp-text-info tracker-email-copy-to-clipboard-info">
            {{# dgettext }}tuleap-tracker | The email has been copied to clipboard{{/ dgettext }}
        </p>
    </div>
    <div class="tlp-modal-footer modal-footer">
        <button
            class="btn tlp-button-primary tlp-button-outline"
            data-dismiss="modal"
            aria-hidden="true"
        >{{# gettext }}Close{{/ gettext }}</button>
    </div>
</div>
