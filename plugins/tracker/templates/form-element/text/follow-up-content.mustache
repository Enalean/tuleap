<button class="btn btn-mini toggle-diff"
        data-changeset-id="{{ changeset_id }}"
        data-field-id="{{ field_id }}"
>
    <i class="fa fa-caret-right show-diff-follow-up" ></i> {{# dgettext }} tuleap-tracker |Show diff{{/ dgettext }}
</button>

<button class="btn btn-mini markup-diff"
        data-changeset-id="{{ changeset_id }}"
        data-field-id="{{ field_id }}"
        id="tracker-changeset-markup-diff-button-{{ changeset_id }}-{{ field_id }}"
>
    <i class="fa fa-random show-markup-diff-follow-up" ></i> {{# dgettext }} tuleap-tracker |Switch to markup diff{{/ dgettext }}
</button>

<div class="diff follow-up-diff"
     id="tracker-changeset-diff-comment-{{ changeset_id }}-{{ field_id }}"
     data-artifact-id="{{ artifact_id }}"
     data-field-id="{{ field_id }}"
></div>

<div class="alert alert-error hide-diff-error-message" id="tracker-changeset-diff-error-{{ changeset_id }}-{{ field_id }}">
    {{# dgettext }} tuleap-tracker | Something went wrong, the follow up content couldn't be loaded {{/ dgettext }}
</div>

<div class="alert alert-info hide-only-formatted-diff-message"
     id="tracker-changeset-only-formatted-diff-info-{{ changeset_id }}-{{ field_id }}"
>
    {{# dgettext }} tuleap-tracker | Only formatting have been changed, you should switch to markup to see the changes {{/ dgettext }}
</div>
