{{# artifact_move_button_presenter }}
    <li>
        <a class="btn tracker-action-button {{# hasError }}disabled{{/ hasError}}"
            id="tracker-action-button-move"
            {{# hasError }}title="{{ errors_content }}"{{/ hasError }}
        >
            <i class="icon-fixed-width icon-share-alt"></i>
            {{ label }}
        </a>
    </li>
{{/ artifact_move_button_presenter }}
