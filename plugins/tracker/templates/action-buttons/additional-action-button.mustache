    <li>
        <a class="btn tracker-action-button"
           {{# url }} href="{{ url }}"{{/ url }}
           {{# disabled }} disabled{{/ disabled }}
        >
            {{{ glyph }}}
            {{ link_label }}
        </a>
    </li>
