{{# artifact_delete_modal_presenter }}
    <div id="delete-artifact-modal-mount-point"
         data-tracker-id="{{ tracker_id }}"
         data-csrf-name="{{ token_name }}"
         data-csrf-token="{{ token }}"
         data-artifact-id="{{ artifact_id }}"
    ></div>
{{/ artifact_delete_modal_presenter }}
