<div id="tracker-admin-current-full-hierarchy">
    <h3>{{#__}}current_full_hierarchy_title{{/__}}</h3>
    {{#hierarchy}}
        <ul class="tracker-hierarchy">{{> tracker-node}}</ul>
    {{/hierarchy}}
</div>
<div class="tracker-admin-hierarchy-edit-form">
    <h3>{{#__}}edit_children_title|{{tracker_name}}{{/__}}</h3>
    <form method="post"
        action="{{getTrackerUrl}}/?tracker={{getTrackerId}}&func=admin-hierarchy-update">
        <select class="tracker-admin-hierarchy-edit-children" name="children[]" multiple="multiple">
            {{#getPossibleChildren}}
            <option value="{{id}}"{{selected}}>{{name}}</option>
            {{/getPossibleChildren}}
        </select>
        <p>
            <input type="submit" value="{{getSubmitLabel}}">
        </p>
    </form>
</div>

