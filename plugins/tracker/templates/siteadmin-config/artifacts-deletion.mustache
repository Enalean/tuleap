<div class="tlp-framed-vertically">
    <h1 class="tlp-framed-horizontally">{{# dgettext }} tuleap-tracker | Trackers {{/ dgettext }}</h1>

    {{# sections }}
        <nav class="tlp-tabs">
            <a class="tlp-tab" href="{{ emailgateway_url }}">{{ emailgateway }}</a>
            <a class="tlp-tab" href="{{ natures_url }}">{{ natures }}</a>
            <a class="tlp-tab" href="{{ report_config_url }}">{{ report_config_panel }}</a>
            <a class="tlp-tab tlp-tab-active" href="{{ artifacts_deletion_url }}">{{# dgettext }} tuleap-tracker | Artifacts deletion {{/ dgettext }}</a>
        </nav>
    {{/ sections }}

    <div class="tlp-framed-horizontally">

        <section class="tlp-pane">
            <div class="tlp-pane-container">
                <div class="tlp-pane-header">
                    <h1 class="tlp-pane-title"><i class="fa fa-trash-o tlp-pane-title-icon"></i>{{# dgettext }} tuleap-tracker | Artifacts deletion configuration {{/ dgettext }}</h1>
                </div>
                <section class="tlp-pane-section">
                    <div class="tlp-alert-danger">
                        <i class="fa fa-exclamation-triangle" aria-hidden="true"></i>&nbsp;
                        {{# dgettext }}
                            tuleap-tracker
                            | Be careful: The limit you define here is the number of artifacts users can delete from the REST API on a 24 hours sliding window. This action is hardly reversible. Choose wisely.
                        {{/ dgettext }}
                    </div>
                    <form method="POST" action="">
                        <input type="hidden" name="action" value="artifacts-deletion-update-limit" />
                        {{# csrf_token }}
                            {{> csrf_token_input }}
                        {{/ csrf_token }}

                        <div class="tlp-form-element">
                            <label class="tlp-label" for="input-artifacts-limit">
                                {{# dgettext }}
                                    tuleap-tracker
                                    | Limit
                                {{/ dgettext }}
                            </label>
                            <input type="number"
                                   id="input-artifacts-limit"
                                   class="tlp-input"
                                   name="artifacts_limit"
                                   placeholder="0"
                                   size="5"
                                   value="{{ artifacts_limit }}"
                                   min="0"
                            />
                        </div>

                        <div class="tlp-pane-section-submit">
                            <button type="submit" class="tlp-button-primary"><i class="fa fa-save tlp-button-icon"></i>
                                 {{# gettext }}Save configuration{{/ gettext }}
                            </button>
                        </div>
                    </form>
                </section>
            </div>
        </section>
    </div>
</div>
