{
  "name": "TQL-grammar",
  "version": "0.1.0",
  "description": "Tuleap Query Language grammar",
  "main": "generate_parser.js",
  "author": "Enalean",
  "license": "GPL-2.0",
  "private": true,
  "dependencies": {
    "pegjs": "^0.10.0",
    "phpegjs": "^1.0.0-beta7"
  },
  "scripts": {
    "build": "rm -rf backend-assets/ && node ./src/generate_parser.js"
  }
}
