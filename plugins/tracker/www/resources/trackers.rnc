namespace xsd = "http://www.w3.org/2001/XMLSchema"

include "tracker-definition.rnc"
include "triggers.rnc"

start = element trackers {
    element tracker {
        tracker
    }*,
    element triggers {
        triggers
    }?
}
