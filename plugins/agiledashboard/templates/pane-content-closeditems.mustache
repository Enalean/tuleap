{{#has_submilestones}}
    <h3>{{closed_items_title}}</h3>

    {{#has_submilestones_done}}
        <p>{{closed_items_intro}}</p>
        <table class="table table-striped table-hover table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>{{title}}</th>
                    <th>{{parent}}</th>
                </tr>
            </thead>
            <tbody>
                {{#done_collection}}
                    <tr data-artifact-id="{{id}}">
                        <td><a href="{{url}}">{{id}}</a></td>
                        <td>{{title}}</td>
                        <td><a href="{{parent_url}}">{{parent_title}}</a></td>
                    </tr>
                {{/done_collection}}
            </tbody>
        </table>
    {{/has_submilestones_done}}
    {{^has_submilestones_done}}
        <p>{{closed_items_nothing_yet}}</p>
    {{/has_submilestones_done}}
{{/has_submilestones}}
