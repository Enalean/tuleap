<div class="tlp-framed-horizontally"
    ng-app="planning"
    ng-controller="MainController"
>
    <div class="planning-init-data" ng-non-bindable
         data-user-id="{{ user_id }}"
         data-project-id="{{ project_id }}"
         data-milestone-id="{{ milestone_id }}"
         data-language="{{ lang }}"
         data-view-mode="{{ view_mode }}"
         data-milestone="{{ milestone_representation }}"
         data-paginated-backlog-items="{{ paginated_backlog_items_representations }}"
         data-paginated-milestones="{{ paginated_milestones_representations }}"
         data-user-accessibility-mode="{{ user_accessibility_mode }}"
         data-is-in-explicit-top-backlog="{{ is_in_explicit_top_backlog }}"
    ></div>
    <planning class="planning"></planning>
</div>
