{{# has_charts }}
    <div class="agiledashboard-milestone-details-graphs">
        {{# has_burndown }}
            <h3>{{ burndown_label }}</h3>
            <img src="{{ burndown_url }}" width="640" height="480">
        {{/ has_burndown }}
        {{# has_burnup }}
            <h3>{{ burnup_label }}</h3>
            {{# burnup_presenter }}
                {{> formelement/burnup-field }}
            {{/ burnup_presenter }}
        {{/ has_burnup }}
    </div>
{{/ has_charts }}
