<div id="art-{{destination_id}}" class="release_planner card planning-droppable">
    <div class="card-link">{{{destination_link}}}</div>
    {{> select-destination}}
    <p class="help">{{destinationHelp}}</p>
    <div>
        {{^getLinkedItems}}
        No items yet
        {{/getLinkedItems}}
        {{#getLinkedItems}}
            {{> cards}}
        {{/getLinkedItems}}
    </div>
</div>
<div style="clear: both"></div>
