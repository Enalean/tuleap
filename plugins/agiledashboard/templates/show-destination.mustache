<div id="art-{{destination_id}}" class="release_planner planning-droppable">
    {{{destination_title}}}
    <p class="help">Drag'n drop content from backlog on the left on this area to add new items to current release.</p>
    <div>
        {{^getLinkedItems}}
        No items yet
        {{/getLinkedItems}}
        <ul>
            {{#getLinkedItems}}
            <li>
                {{getTitle}}
            </li>
            {{/getLinkedItems}}
        </ul>
    </div>
</div>
<div style="clear: both"></div>
