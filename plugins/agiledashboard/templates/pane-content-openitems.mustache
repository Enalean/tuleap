{{#has_something}}
    <h3>{{#has_something_todo}}All{{/has_something_todo}}{{^has_something_todo}}No more{{/has_something_todo}} open {{backlog_item_type}}{{^has_something_todo}}, well done!{{/has_something_todo}}</h3>
{{/has_something}}
{{^has_something}}
    <h3>No {{backlog_item_type}}{{#can_add_backlog_item}}, start adding now{{/can_add_backlog_item}}.</h3>
{{/has_something}}

{{#can_add_backlog_item}}
<p>
    <a href="{{add_new_backlog_url}}" class="btn">{{add_new_backlog_item}}</a>
</p>
{{/can_add_backlog_item}}

{{#has_something}}
    {{#has_something_todo}}
        <p>Here are the items that are still open. You can prioritize by drag 'n
           dropping them.</p>
        <table class="table table-striped table-hover table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>{{title}}</th>
                    <th>{{points}}</th>
                    <th>{{parent}}</th>
                </tr>
            </thead>
            <tbody class="milestone-content-rows">
                {{#todo_collection}}
                    <tr data-artifact-id="{{id}}">
                        <td><a href="{{url}}">{{id}}</a></td>
                        <td>{{title}}</td>
                        <td>{{points}}</td>
                        <td><a href="{{parent_url}}">{{parent_title}}</a></td>
                    </tr>
                {{/todo_collection}}
            </tbody>
        </table>
    {{/has_something_todo}}
{{/has_something}}