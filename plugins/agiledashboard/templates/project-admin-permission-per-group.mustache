<section class="tlp-pane">
    <div class="tlp-pane-container">
        <div class="tlp-pane-header">
            <h1 class="tlp-pane-title"> {{# dgettext }} tuleap-agiledashboard | Agiledashboard plannings {{/ dgettext }}</h1>
        </div>

        <section class="tlp-pane-section">
            <table class="tlp-table">
                <thead>
                <tr>
                    <th> {{# dgettext }} tuleap-agiledashboard | Planning {{/ dgettext }}</th>
                    <th> {{# dgettext }} tuleap-agiledashboard | Who can prioritize? {{/ dgettext }}</th>
                </tr>
                </thead>
                <tbody>
                {{# permissions }}
                    <tr>
                        <td>
                            <a href="{{ quick_link }}">{{ name }}</a>
                        </td>
                        <td>
                            {{# groups }}
                                <span class="
                                    {{# is_project_admin }}tlp-badge-danger tlp-badge-outline{{/ is_project_admin }}
                                    {{# is_static }}tlp-badge-primary tlp-badge-outline{{/ is_static }}
                                    {{# is_custom }}tlp-badge-secondary{{/ is_custom }}
                                ">{{ name }}</span>
                            {{/ groups }}
                        </td>
                    </tr>
                {{/ permissions }}
                {{^ has_permissions }}
                    <tr>
                        <td colspan="2" class="tlp-table-cell-empty">
                            {{# dgettext }} tuleap-agiledashboard | {{ user_group_name }} has no permission for agiledashboard plannings {{/ dgettext }}
                        </td>
                    </tr>
                {{/ has_permissions }}
                </tbody>
            </table>
        </section>
    </div>
</section>
