{{# are_all_backlog_trackers_missconfigured }}
    <ul class="feedback_error">
        {{# dngettext }}
            tuleap-velocity |
            Artifact velocity will always be set to 0, because no backlog tracker have the following semantic: |
            Artifact velocity will always be set to 0, because no backlog tracker have the following semantics: |
            {{ nb_semantic_misconfigured }}
        {{/ dngettext }}
        {{# misconfigured_semantics }}
            <b>{{{ . }}}</b><span class="misconfigured-semantic-display-comma">, </span>
        {{/ misconfigured_semantics }}
    </ul>
{{/ are_all_backlog_trackers_missconfigured }}