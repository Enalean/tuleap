{{^ has_at_least_one_well_configured_children }}
    <ul class="feedback_error">
        {{# dngettext }}
            tuleap-velocity |
            Following tracker should have <b>velocity</b> semantic: |
            Following trackers should have <b>velocity</b> semantic: |
            {{ nb_children_without_velocity_semantic }}
        {{/ dngettext }}
        {{# children_misconfigured_semantic }}
            <a href="{{ tracker_url }}">{{ name }}</a><span class="misconfigured-semantic-display-comma">, </span>
        {{/ children_misconfigured_semantic }}
    </ul>
{{/ has_at_least_one_well_configured_children }}