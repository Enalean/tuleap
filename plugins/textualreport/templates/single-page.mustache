<!DOCTYPE html>
<meta charset="utf-8">
<style>{{{ stylesheets }}}</style>
<body class="tlp-framed">
{{# artifacts }}
    <section class="tlp-pane">
        <div class="tlp-pane-container">
            <div class="tlp-pane-header">
                <h1 class="tlp-pane-title">
                    <a href="{{ href }}">{{ xref }}</a>
                    {{ title }}
                </h1>
            </div>
            <section class="tlp-pane-section">
                <div>
                    {{{ purified_description }}}
                </div>
            </section>
        </div>
    </section>
{{/ artifacts }}
{{# is_hard_limit_reached }}
    <div class="tlp-alert-warning">
        {{# dgettext }}
            tuleap-textualreport
            | There are too many matching artifacts (%s), for performance
              reasons we cannot generate a document with more than %s artifacts.
            | {{ nb_matching_artifacts }}
            | {{ hard_limit }}
        {{/ dgettext }}
    </div>
{{/ is_hard_limit_reached }}
</body>
