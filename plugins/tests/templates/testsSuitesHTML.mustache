<li class="{{classes}}">
    <label for="{{name}}">{{title}}</label>
    <input type="checkbox" name="{{name}}" value="{{value}}" {{checked}}/>
    <ul id="{{id}}" class="{{classes}}">
    {{#children}}
        {{> testsSuitesHTML}}
    {{/children}}
    </ul>
</li>